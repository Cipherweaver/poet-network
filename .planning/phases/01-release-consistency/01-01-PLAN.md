---
phase: 01-release-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - README.md
  - pipeline/build-data.mjs
  - pipeline/lib/build-graph.mjs
  - pipeline/lib/build-graph.test.mjs
  - src/data/poetGraph.ts
autonomous: true
requirements:
  - RELI-01
  - RELI-02
user_setup: []
must_haves:
  truths:
    - "Release builds use committed public graph assets instead of regenerating data from machine-local source directories"
    - "The data pipeline writes only runtime-used JSON artifacts under public/"
    - "Project documentation tells maintainers to refresh graph assets explicitly before committing or deploying data changes"
  artifacts:
    - path: "package.json"
      provides: "Deterministic release build scripts"
      contains: "\"build\": \"tsc -b && vite build\""
    - path: "pipeline/build-data.mjs"
      provides: "JSON-only graph generation"
      contains: "public/graph.json"
    - path: "README.md"
      provides: "Documented release asset workflow"
      contains: "npm run build:data"
  key_links:
    - from: "package.json"
      to: "public/graph.json"
      via: "Build documentation and explicit maintainer workflow"
      pattern: "build:data"
    - from: "pipeline/build-data.mjs"
      to: "public/poems"
      via: "JSON write operations"
      pattern: "poemsOutputDir"
---

<objective>
Stabilize the release data contract and remove the unused generated graph module.

Purpose: Ensure deployed builds are reproducible across machines and that the project maintains a single source of truth for runtime data.
Output: Updated build scripts, narrowed data pipeline outputs, refreshed pipeline tests, and aligned maintainer documentation.
</objective>

<execution_context>
@C:/Users/14169/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/14169/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-release-consistency/01-CONTEXT.md
@.planning/phases/01-release-consistency/01-RESEARCH.md
@package.json
@README.md
@pipeline/build-data.mjs
@pipeline/lib/build-graph.mjs
@pipeline/lib/build-graph.test.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make release builds deterministic</name>
  <files>package.json, README.md</files>
  <read_first>package.json, README.md, .planning/ROADMAP.md, .planning/phases/01-release-consistency/01-RESEARCH.md</read_first>
  <action>Change `package.json` so the `build` script becomes exactly `tsc -b && vite build`, leaving `build:data` as the explicit asset regeneration command. Update README sections for development and Vercel deployment so they state that maintainers must run `npm run build:data` locally when source poetry data or aliases change, then commit the refreshed `public/graph.json` and `public/poems/*.json` assets before running or relying on `npm run build` in CI/deploy environments.</action>
  <verify>Get-Content package.json; Get-Content README.md</verify>
  <acceptance_criteria>
    - `package.json` contains `"build": "tsc -b && vite build"`
    - `package.json` still contains `"build:data": "node ./pipeline/build-data.mjs"`
    - `README.md` describes `npm run build:data` as an explicit maintainer step before deploy when data changes
  </acceptance_criteria>
  <done>Release build no longer regenerates data implicitly, and the release asset workflow is documented.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the unused TypeScript graph output from the pipeline</name>
  <files>pipeline/build-data.mjs, pipeline/lib/build-graph.mjs, pipeline/lib/build-graph.test.mjs, src/data/poetGraph.ts</files>
  <read_first>pipeline/build-data.mjs, pipeline/lib/build-graph.mjs, pipeline/lib/build-graph.test.mjs, src/App.tsx, README.md</read_first>
  <action>Delete the `src/data/poetGraph.ts` output path from `pipeline/build-data.mjs`, remove the now-unused `serializeGraphModule` export and any related assertions from `pipeline/lib/build-graph.mjs` and `pipeline/lib/build-graph.test.mjs`, and delete the generated file `src/data/poetGraph.ts` from the repository. Keep JSON graph output in `public/graph.json` and per-poet JSON output in `public/poems/*.json` unchanged so runtime fetch behavior remains intact.</action>
  <verify>npm run test -- pipeline/lib/build-graph.test.mjs</verify>
  <acceptance_criteria>
    - `pipeline/build-data.mjs` no longer contains `src/data/poetGraph.ts`
    - `pipeline/lib/build-graph.mjs` no longer exports `serializeGraphModule`
    - `pipeline/lib/build-graph.test.mjs` no longer asserts `export const poetGraph`
    - `src/data/poetGraph.ts` does not exist
  </acceptance_criteria>
  <done>The pipeline produces only runtime-used JSON assets, and obsolete graph module generation is removed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test` passes
- [ ] `npm run build` passes without regenerating data during the `build` script itself
- [ ] `README.md` and `package.json` describe the same release asset workflow
</verification>

<success_criteria>

- All tasks completed
- Release build no longer depends on machine-local poetry source directories
- The project has exactly one runtime graph data contract under `public/`
  </success_criteria>

<output>
After completion, create `.planning/phases/01-release-consistency/01-01-SUMMARY.md`
</output>

---
phase: 01-release-consistency
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - package.json
  - vite.config.ts
  - src/App.tsx
  - src/components/Sidebar.tsx
  - src/lib/poems.ts
  - src/lib/poems.test.ts
  - src/App.test.tsx
  - src/components/Sidebar.test.tsx
  - src/test/fixtureGraph.ts
autonomous: true
requirements:
  - LOAD-01
  - TEST-01
user_setup: []
must_haves:
  truths:
    - "Graph loading success and failure produce explicit, test-covered UI states"
    - "Poem modal open/close behavior is controlled by React state instead of DOM queries"
    - "Poem JSON failures fall back to graph-derived relation data with automated regression coverage"
  artifacts:
    - path: "src/App.tsx"
      provides: "Explicit graph loading and keyboard interaction state"
      contains: "poemModal"
    - path: "src/components/Sidebar.tsx"
      provides: "Explicit poem modal state notifications and tested fallback behavior"
      contains: "buildFallbackPoems"
    - path: "src/App.test.tsx"
      provides: "Graph loading and Escape interaction regression tests"
      min_lines: 20
    - path: "src/components/Sidebar.test.tsx"
      provides: "Poem fallback regression tests"
      min_lines: 20
  key_links:
    - from: "src/App.tsx"
      to: "src/components/Sidebar.tsx"
      via: "modal state callback props"
      pattern: "on.*Modal.*Change"
    - from: "src/App.test.tsx"
      to: "src/App.tsx"
      via: "component render and interaction assertions"
      pattern: "render\\("
---

<objective>
Refactor loading and modal state into explicit React-driven paths and add regression tests for the high-risk interactions.

Purpose: Remove brittle DOM-coupled behavior and establish automated protection for the loading and keyboard flows touched in Phase 1.
Output: Updated App/Sidebar state flow, component-test setup, and regression tests for graph loading, poem fallback, and Escape handling.
</objective>

<execution_context>
@C:/Users/14169/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/14169/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-release-consistency/01-CONTEXT.md
@src/App.tsx
@src/components/Sidebar.tsx
@src/lib/poems.ts
@src/lib/poems.test.ts
@src/test/fixtureGraph.ts
@vite.config.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace DOM-query modal gating with explicit state</name>
  <files>src/App.tsx, src/components/Sidebar.tsx</files>
  <read_first>src/App.tsx, src/components/Sidebar.tsx, .planning/phases/01-release-consistency/01-CONTEXT.md</read_first>
  <action>Add an explicit boolean state in `App` for whether the poem modal is open, pass a callback prop into `Sidebar` so it notifies `App` when the expanded poem modal opens or closes, and update the `Escape` handling logic so it only clears `selectedId` when no poem modal is currently open. Remove the `document.querySelector('.poem-modal-backdrop')` check entirely.</action>
  <verify>Get-Content src/App.tsx; Get-Content src/components/Sidebar.tsx</verify>
  <acceptance_criteria>
    - `src/App.tsx` does not contain `document.querySelector('.poem-modal-backdrop')`
    - `src/App.tsx` contains explicit modal-open state used by the `Escape` handler
    - `src/components/Sidebar.tsx` exposes a prop or callback that reports poem modal open/close changes
  </acceptance_criteria>
  <done>Modal visibility is represented by React state rather than inferred from DOM structure.</done>
</task>

<task type="auto">
  <name>Task 2: Add component-test coverage for loading and fallback flows</name>
  <files>package.json, vite.config.ts, src/App.test.tsx, src/components/Sidebar.test.tsx, src/lib/poems.test.ts, src/test/fixtureGraph.ts</files>
  <read_first>package.json, vite.config.ts, src/lib/poems.test.ts, src/test/fixtureGraph.ts, src/App.tsx, src/components/Sidebar.tsx</read_first>
  <action>Add the minimum component-test tooling required for Vitest to render React components in jsdom, then create `src/App.test.tsx` to cover graph load success, graph load failure, and `Escape` behavior when the poem modal is closed versus open. Create `src/components/Sidebar.test.tsx` to cover poem fetch failure falling back to `buildFallbackPoems`. Reuse `fixtureGraph` instead of introducing a second graph fixture format, and keep existing helper tests in `src/lib/poems.test.ts` aligned with the new behavior.</action>
  <verify>npm run test</verify>
  <acceptance_criteria>
    - `package.json` contains any new test dependencies needed for React component tests
    - `vite.config.ts` or equivalent Vitest config sets `jsdom` for component tests
    - `src/App.test.tsx` asserts both graph-load states and `Escape` behavior
    - `src/components/Sidebar.test.tsx` asserts fallback data is shown when poem fetch fails
  </acceptance_criteria>
  <done>The risky loading and keyboard paths are protected by component-level regression tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test` passes
- [ ] `npm run lint` passes
- [ ] New component tests fail if the modal gating or fallback behavior regresses
</verification>

<success_criteria>

- All tasks completed
- Graph and poem loading behavior are explicit and testable
- Phase 1 high-risk UI interactions have automated regression coverage
  </success_criteria>

<output>
After completion, create `.planning/phases/01-release-consistency/01-02-SUMMARY.md`
</output>
